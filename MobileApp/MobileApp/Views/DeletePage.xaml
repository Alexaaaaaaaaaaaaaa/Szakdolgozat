<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.Views.DeletePage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Miért törlöd" FontSize="Medium" FontAttributes="Bold" HorizontalTextAlignment="Center" Margin="0,20,0,0"/>
            <Label Text= "{Binding DeleteText}" FontSize="Medium" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
            <Label Text="nevű élelmiszert?" FontSize="Medium" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Text="Megromlott" Command="{Binding ItemWasted}" Grid.Column="0" BackgroundColor="LightCoral"/>
                <Button Text="Elfogyott" Command="{Binding ItemUsed}" Grid.Column="1"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>